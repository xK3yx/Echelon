# Echelon v2

AI-assisted career intelligence tool. You describe your skills, personality, and interests; Echelon ranks career paths against your profile using a deterministic rule engine followed by a Groq LLM re-ranker, then generates per-career skill gap analyses and personalised learning roadmaps.

Built as a final-year diploma portfolio project.

---

## Honest disclaimers

- **Portfolio project** — this is an academic portfolio piece, not a production product. It has not been validated by career counsellors or industry research.
- **Curated dataset of 20 careers** — the career database is a hand-crafted set of 20 tech-adjacent paths. It does not cover all career options and should not be treated as comprehensive.
- **LLM non-determinism** — rankings, reasoning, and roadmaps are generated by the Groq API (`llama-3.3-70b-versatile`). Identical inputs may produce slightly different outputs across runs. "Re-run" intentionally exploits this.
- **No authentication** — each profile submission creates an anonymous user row. There is no login, session, or access control. Do not submit real personal data.
- **Not career advice** — output is illustrative. Consult a human career advisor before making major decisions.

---

## Architecture

```
Browser (Next.js 14)
    │  POST /api/profiles       → create profile
    │  POST /api/recommendations → trigger AI pipeline
    │  GET  /api/recommendations/:id → poll result
    ▼
FastAPI (Python 3.11)
    │
    ├─ Stage 1: Rule scoring (deterministic)
    │    skill × 0.45 + optional × 0.15 + personality × 0.25 + interest × 0.15
    │    → top 10 candidates
    │
    ├─ Stage 2: Groq re-rank (llama-3.3-70b-versatile, temp 0.3)
    │    → top 5 with fit reasoning, strengths, risks, confidence
    │
    └─ Stage 3: Groq gap + roadmap (llama-3.3-70b-versatile, temp 0.2)
         → skill gaps (easy/medium/hard) + 3-phase learning roadmap per career
    │
    ▼
PostgreSQL 15
    users  →  profiles  →  recommendations (result JSONB)
                        →  careers (seed data)
```

**Key design decisions:**

- Two Groq calls total per recommendation (re-rank + gap/roadmap batched together), not one per career.
- Results cached by `profile_id` in the `recommendations` table. `refresh=true` bypasses the cache.
- LLM responses validated with Pydantic v2; on parse failure the call is retried once with a stricter prompt at lower temperature.
- The rule scorer has zero LLM dependency and is fully unit-tested.

---

## Tech stack

| Layer | Technology |
|---|---|
| Frontend | Next.js 14.2 · TypeScript strict · Tailwind CSS 3.4 · shadcn/ui · TanStack Query v5 · React Hook Form + Zod |
| Backend | Python 3.11 · FastAPI · SQLAlchemy 2 async · Alembic · Pydantic v2 |
| Database | PostgreSQL 15 |
| AI | Groq API — `llama-3.3-70b-versatile` (ranking/roadmap) · `llama-3.1-8b-instant` (extraction) |
| Infrastructure | Docker Compose · uvicorn hot-reload |

---

## Prerequisites

- Docker Desktop (with Compose v2)
- Groq API key — free tier at [console.groq.com](https://console.groq.com)
- Git

---

## Setup

### 1 — Clone

```bash
git clone https://github.com/xK3yx/Echelon.git
cd Echelon
```

### 2 — Environment

```bash
cp .env.example .env
```

Open `.env` and set:

```env
DATABASE_URL=postgresql+asyncpg://echelon:echelon@postgres:5432/echelon
GROQ_API_KEY=your_key_here
DEBUG=false
NEXT_PUBLIC_API_URL=http://localhost:8000/api
```

### 3 — Start services

```bash
make up
```

This builds both images and starts postgres, backend, and frontend.
First build takes ~2 minutes; subsequent starts are instant.

### 4 — Run migrations

```bash
make migrate
```

### 5 — Seed career data

```bash
make seed
```

Loads 20 curated career records. Safe to run multiple times (skips existing slugs).

### 6 — Open the app

| Service | URL |
|---|---|
| Frontend | http://localhost:3000 |
| Backend API | http://localhost:8000/api |
| API docs | http://localhost:8000/docs |

---

## Environment variables

| Variable | Required | Description |
|---|---|---|
| `DATABASE_URL` | Yes | asyncpg connection string — set automatically by Compose for the backend container |
| `GROQ_API_KEY` | Yes | Groq API key. Without it, `/recommendations` returns 502. |
| `DEBUG` | No | Enables FastAPI debug mode (default `false`) |
| `NEXT_PUBLIC_API_URL` | Yes (frontend) | Base URL the browser uses to reach the API |

---

## Make targets

```
make up       # build images + start stack in background
make down     # stop and remove containers
make migrate  # run Alembic migrations to head
make seed     # seed 20 career records
make test     # run pytest suite inside backend container
make lint     # ruff + black --check inside backend container
```

---

## API reference (summary)

```
POST   /api/profiles                   create profile + anonymous user
GET    /api/profiles/:id               fetch profile
GET    /api/profiles/:id/matches       rule-score only (debug endpoint)

GET    /api/careers                    list all 20 careers
GET    /api/careers/:slug              single career

POST   /api/recommendations            run AI pipeline (cached by profile_id)
GET    /api/recommendations/:id        fetch saved recommendation

POST   /api/analyze                    per-career skill gap deep-dive

GET    /api/health                     {"status":"ok","version":"2.0.0"}
```

All errors follow the shape: `{"error": {"code": "STRING_CODE", "message": "...", "details": {}}}`.

---

## Limitations

- **20 careers only.** Paths outside the seeded set will never appear in results.
- **English only.** Skill and interest matching is simple substring/set overlap — no NLP, no synonyms.
- **No account system.** Closing the browser loses the profile ID. Bookmark your results URL.
- **Groq rate limits.** The free tier allows ~30 requests/min. Heavy testing may trigger 429 errors.
- **LLM hallucination.** Roadmap skills and project ideas are generated text. Verify them independently.
- **Personality model is illustrative.** The Big Five sliders feed a heuristic formula; they are not a validated psychometric instrument.

---

## Running tests locally (without Docker)

```bash
cd backend
pip install -e ".[dev]"
# Requires a running Postgres — set DATABASE_URL in your shell
pytest tests/ -v
```

---

## Project structure

```
Echelon/
├── backend/
│   ├── app/
│   │   ├── llm/          # Groq client, prompt builders, Pydantic validators
│   │   ├── models/       # SQLAlchemy ORM models
│   │   ├── routers/      # FastAPI route handlers
│   │   ├── schemas/      # Pydantic request/response models
│   │   ├── seed/         # careers.json + seed script
│   │   └── services/     # matching, ranker, gap_analyzer, roadmap
│   ├── alembic/          # migrations
│   └── tests/
├── frontend/
│   └── src/
│       ├── app/          # Next.js App Router pages
│       ├── components/   # UI components (shadcn/ui + TagInput)
│       └── lib/          # api client, types, schemas, utils
├── docker-compose.yml
├── Makefile
└── .env.example
```
